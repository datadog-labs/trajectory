# Trajectory

Trajectory is capturing this session for observability. The trajectory MCP server provides introspection tools.

Available commands:
- `trajectory status` - check capture status
- `trajectory doctor` - diagnose issues
- `trajectory config show` - view configuration
- `/incognito` - suppress publish to non-exempt Datadog destinations for this session while local JSONL capture continues
